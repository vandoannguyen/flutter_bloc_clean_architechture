import 'package:flutter/material.dart';

class DemoCommonWidget extends StatelessWidget {
  const DemoCommonWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
