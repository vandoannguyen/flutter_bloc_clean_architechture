import 'package:flutter/material.dart';

class DemoCommonWidget extends StatelessWidget {
  const DemoCommonWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
